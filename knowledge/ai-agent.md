# AI Agent

AI Agent 是围绕大模型构建的任务执行循环。模型负责理解、推理和生成文本，工程代码负责准备上下文、选择能力、调用工具、保存观察结果，并决定什么时候结束。

一个最小 Agent Loop 通常包含：

1. 接收用户输入。
2. 读取系统 Prompt、历史消息和任务说明。
3. 按需要检索知识或选择 Agent Skill。
4. 把上下文交给模型。
5. 如果模型要求调用工具，执行工具并把 observation 回填。
6. 让模型生成最终回答。

Agent 的关键不在于模型 API 调用本身，而在于如何组织上下文和外部能力。
